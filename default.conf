server {
    listen 8000;
    server_name test-quant.vaneck.com;

    location ./ {
        root /usr/share/nginx/html;
        try_files $uri ./index.html;
    }

    location / {
        root /usr/share/nginx/html;
        try_files $uri /index.html;
    }
}
        